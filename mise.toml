# SPDX-License-Identifier: MPL-2.0
# Only tools used by this repository; Rust is pinned in rust-toolchain.toml.
[tools]
bun = "1.3.14"

[tasks.test]
run = 'bash tests/check_alignment.sh "${GROOVE_CHECKOUT:?Set the Groove checkout to verify}"'
